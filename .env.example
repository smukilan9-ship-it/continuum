# Supabase (public values are safe to expose; service credentials are server-only)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=

# AI Gateway / providers (server-only)
AI_GATEWAY_API_KEY=
FEATHERLESS_API_KEY=
GEMINI_API_KEY=
GROQ_API_KEY=
EMBEDDING_MODEL=

# MCP OAuth (server-only except issuer URL)
MCP_OAUTH_ISSUER_URL=
MCP_JWT_SIGNING_SECRET=
MCP_DEMO_TOKEN=

# App
APP_BASE_URL=http://localhost:3000
FEATURE_FLAGS=demo_token
PER_USER_DAILY_TOKEN_CAP=50000
