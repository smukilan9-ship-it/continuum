# Premium visual QA

Date: 2026-07-23  
Reference: the supplied warm off-white, charcoal, acid-lime, and muted-olive
editorial direction. No crypto branding, composition, or assets were copied.

## System reviewed

- Semantic light/dark tokens; persisted `system`, `light`, and `dark`
  preference; pre-hydration theme bootstrap.
- Restrained 160 ms color/transform transitions and reduced-motion overrides.
- Editorial Today hierarchy, flat state ledger, compact shell, pill actions,
  sparse dividers, and grouped Connections settings rows.
- Provider editor disclosure, invalid-key feedback, NotebookLM tier labels, and
  mobile-safe bottom navigation.

## Evidence

The prior blue/card-heavy baseline remains under `docs/audit-screenshots/`.
The automated final matrix is generated by
`scripts/visual-qa-premium.mjs` into `docs/audit-screenshots/premium/` for:

- 1440×900
- 1280×800
- 390×844
- 375×812

It covers Login, Today, Plan, Learn, Code, Research, Memory, Review, and
Connections in light and dark modes and records horizontal-overflow checks in
`qa-results.json`.

## Manual interaction review

- Demo login completed and landed on the seeded Today state.
- Theme changed from system-dark to light, persisted across navigation, and
  updated the mobile top-bar control without hydration flash.
- Today and Connections were visually reviewed in both themes.
- Credential configuration expanded inline without shifting into a modal or
  exposing a secret.
- A deliberately invalid Featherless key was rejected with an actionable toast
  and was not stored.
- Mobile primary content cleared the fixed bottom navigation; the theme control
  remained available at the compact breakpoint.

No visual result is treated as provider-connectivity evidence.
