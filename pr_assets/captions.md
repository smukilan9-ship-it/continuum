# Image captions

One line per capture, each under 140 characters, in upload order.

| File | Caption |
|---|---|
| `01-home-today.png` | Home. One next action with the reason it was chosen, today's agenda, and a week the solver built around real deadlines. |
| `02-plan-week.png` | The week grid. Every block placed by a constraint solver against deadlines, prerequisites and fixed commitments. No model called. |
| `03-goal-concept-map.png` | A goal's concept map: prerequisites, what each concept unlocks, and mastery and confidence tracked as separate numbers. |
| `04-learn-status.png` | Study status. Each concept shows the evidence behind its mastery instead of a completion percentage, and names open misconceptions. |
| `05-review-proposals.png` | Review. Every AI-proposed change waits here as a field-level diff with a risk label. Nothing lands without approval. |
| `06-library-sources.png` | Library. Imported sources with processing state, chunk counts and retention. A chunk is the unit a citation points at. |
| `07-library-discover.png` | Discover, running a live OpenAlex query. Saving a result indexes its passages, so later answers can cite the paper directly. |
| `08-library-citation-graph.png` | A paper opened with its references, citations and related work, so the literature around a claim is one hop away. |
| `09-ask-grounded.png` | A grounded answer: two accepted decisions and a source passage, each an openable citation chip rather than a claim to trust. |
| `10-context-routing.png` | Context. Every routing decision is recorded with the model chosen, the reason it was taken, and what verified the output. |
| `11-research-projects.png` | Research. Claims linked to the passages that support or contradict them, alongside accepted decisions and open questions. |
| `12-build-console.png` | Build. A sandboxed editor and console, where runtime output reaches the model as evidence and never as instruction. |
| `13-command-palette.png` | One palette searching goals, projects, sources, passages and memory. Opening a result never changes data. |
| `14-settings-connections.png` | Connections. Bring your own key for every provider, and revoke any connected agent from the same screen. |
| `15-mcp-authorize.png` | The MCP consent screen. Each scope is a plain-English checkbox badged read-only or can-make-changes. PKCE, short-lived, revocable. |
